# Node 16 is the newest pre-18 runtime with darwin-arm64 builds; the deployed
# Lambda runtime is still nodejs14.x. Both move to 22 with the osls/esbuild flip.
[tools]
node = "16"
pnpm = "11"
java = "temurin-21"
