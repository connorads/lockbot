apis:
  lockbot@v1:
    root: ./src/handlers/swagger/openapi.json

extends:
  - recommended

rules:
  # Servers added at Swagger UI runtime
  no-empty-servers: off
  # Ignore warnings
  operation-operationId: off
  info-license: off
